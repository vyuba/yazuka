<section class="shop-page">
  <div class="banner ">
    <div class="img-banner2"></div>
    <div class="logo">
      <img src="https://cdn.shopify.com/s/files/1/0552/4511/1342/files/YAZUKA.png?v=1674193263" alt="" height=""
        width="">
    </div>
    <div class="collection-container">
      <a href="{{ routes.all_products_collection_url }}">
        <li>
          collection
        </li>
      </a>
    </div>
  </div>
</section>

<style>
  .shop-page {
    width: 100%;
    height: 100%;
  }

  .banner {
    position: relative;
    width: 100%;
    height: 600px;
    color: white;
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: flex-end;
  }

  .banner::after {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background: linear-gradient(to top, rgba(0, 0, 0, 0.5), transparent);
    pointer-events: none;
  }


  .img-banner2 {
    position: absolute;
    top: 0;
    /* Ensure it starts at the top */
    left: 0;
    /* Ensure it starts from the left */
    right: 0;
    /* Ensure it spans the full width */
    bottom: 0;
    /* Ensure it spans the full height */
    background-image: url('{{ "IMG_7048.JPG" | asset_url }}');
    /* Use the URL as a background image */
    background-size: cover;
    /* Scale the image to cover the container */
    background-position: center;
    /* Align the image to the top */
    background-repeat: no-repeat;
    /* Prevent the image from repeating */
  }

  .banner-container {
    z-index: 1000;
  }

  .logo {
    font-size: 100px;
    text-transform: capitalize;
    position: fixed;
    color: white;
    z-index: 1;
    top: 50%;
    left: 50%;
    transform: translate(-50%, -50%);
  }

  .collection-container {
    z-index: 1;
    font-size: 15px;
    font-weight: 800;
    color: white;
    display: flex;
    flex-direction: row;
    gap: 10px;
    padding-bottom: 20px;
    text-transform: uppercase;
  }

  .products {
    width: 100%;
    height: fit-content;
    background-color: #D0D0D0;
    display: flex;
    align-items: center;
    justify-content: center;
  }

  .new-arrivals {
    width: 100%;
    height: 100%;
    background-color: #FAF8F5;
    z-index: 2;
    position: relative;
  }

  .new-products-in-mobile-view {
    display: grid;
  }

  .new-products-in-laptop-view {
    display: none;
  }


  @media (min-width: 1024px) {
    .img-banner2 {
      object-fit: center;
      background-position: center 30%;
    }

    .banner {
      height: calc(100vh - 120px);
    }

    .new-products-in-laptop-view {
      display: grid;
    }

    .new-products-in-mobile-view {
      display: none;
    }
  }
</style>